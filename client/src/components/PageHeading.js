import React from "react";

const PageHeading = (props) => {
    return (
        <div className="page-heading">
            <h2>{props.heading}</h2>
        </div>
    );
};

export default PageHeading;
